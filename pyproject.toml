[project]
name = "jobpilot"
version = "0.0.1"
description = "JobPilot v1.0 - Day 1 foundation"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "langchain-openai>=1,<2",
    "pydantic>=2,<3",
    "pydantic-settings>=2,<3",
    "pypdf>=5,<7",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
extend-exclude = ["*.md"]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
